import React from "react";
import ReactDOM from "react-dom/client";
import GrowthOS from "./GrowthOS.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <GrowthOS />
  </React.StrictMode>
);
